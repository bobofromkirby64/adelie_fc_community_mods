# How to run
Run with `./run.bat`
This will run the main game executable, but also launch the server executable first if it's not already running. Run it a second time to connect a second player.
Once both players launch, click on the server window `netcodeServer` and press enter to start the match.

Need a mac/linux user to make a bash version of this :)